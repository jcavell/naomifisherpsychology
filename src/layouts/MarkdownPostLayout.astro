---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter, active } = Astro.props;
---

<BaseLayout active = {active} pageTitle={frontmatter.title}>
  <img src={frontmatter.image.url} width="300" alt={frontmatter.image.alt} />
  <slot />
</BaseLayout>
