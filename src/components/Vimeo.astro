---
const { embedUrl } = Astro.props;
---

{
  embedUrl && (
    <iframe
      src={embedUrl}
      width="540"
      height="300"
      frameborder="2"
      allow="autoplay; fullscreen; picture-in-picture; clipboard-write"
      title="Trailer"
    />
  )
}
